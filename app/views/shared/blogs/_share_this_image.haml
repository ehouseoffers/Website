:ruby
  # where we setup the textarea highlighting on focus
  extra_js('blog')

  copy_code = render 'shared/blogs/copy_image_code_create', :blog => blog

%div.box
  %div.gutter
    %strong Use This Image

    %textarea.copy_image_code= copy_code
    %div.footnote
      Add this image to your website by copying &amp; pasting the above HTML code
    -#= render 'shared/clippy', :text => copy_code
    -#= image_tag '/images/transparent.gif', :id => 'copy_embed_code_btn', :class => 'reasons_sprite', :alt => 'Copy Embed Code'



