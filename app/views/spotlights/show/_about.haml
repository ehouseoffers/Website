:ruby
  return if spotlight.bullet_points.empty?

%div#aboutC
  = admin_area do
    = link_to_edit_collection_for(:bullet_points, spotlight)

  %h2.green= spotlight.about_title

  %ul.green
    - spotlight.bullet_points.each do |li|
      %li{:id => "about_#{li.id}"}
        %span= li.content